apiVersion: v1
kind: Service
metadata:
    name: usermanagementservice
    namespace: user-management
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: usermanagementui