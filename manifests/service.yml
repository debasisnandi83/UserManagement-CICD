apiVersion: v1
kind: Service
metadata:
    name: debasisnandiusermanagementcicd
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: debasisnandiusermanagementcicd